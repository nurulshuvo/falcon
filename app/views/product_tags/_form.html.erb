<%= form_for(@product_tag) do |f| %>
  <% if @product_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_tag.errors.count, "error") %> prohibited this product_tag from being saved:</h2>

      <ul>
      <% @product_tag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tag_id %><br />
    <%= f.number_field :tag_id %>
  </div>
  <div class="field">
    <%= f.label :product_id %><br />
    <%= f.number_field :product_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
